{
  "name": "variant-loom",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "异文织机 — 本地 XML 见证本校勘工具",
  "scripts": {
    "dev": "tsx src/server/index.ts",
    "build": "tsc -p tsconfig.json",
    "test": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
